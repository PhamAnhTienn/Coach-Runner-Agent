version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    container_name: runnershop
    volumes:
      - .:/app
    networks:
      - chatbot

networks:
  chatbot:
    driver: bridge
